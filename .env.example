# ============================================================================
# AI PROVIDER CONFIGURATION
# ============================================================================

# Choose either "anthropic" or "openai"
AI_PROVIDER=openai

# AI API Keys (only one is required based on AI_PROVIDER)
ANTHROPIC_API_KEY=your_anthropic_api_key_here
OPENAI_API_KEY=your_openai_api_key_here

# Model Configuration (optional - uses provider defaults if not set)
AI_MODEL=gpt-4o
AI_TEMPERATURE=0.7
MAX_TOKENS=4096

# ============================================================================
# PROMPT CUSTOMIZATION (optional)
# ============================================================================

# Add company/team context to improve AI understanding
COMPANY_CONTEXT=We build web and mobile apps for clients. Our team uses React Native, Node.js, and PostgreSQL.

# Describe the PM role at your company
PM_ROLE_DESCRIPTION=Product Managers coordinate client projects and manage scope, timeline, and budget.

# Override entire system prompt (advanced - leave empty to use default with context above)
SYSTEM_PROMPT_OVERRIDE=

# Override extraction prompt (advanced - leave empty to use default)
EXTRACTION_PROMPT_OVERRIDE=

# ============================================================================
# SLACK INTEGRATION
# ============================================================================

# Team Slack Handles - Format: "Real Name:@slack-handle, Another Name:@another-handle"
# When action items are assigned to these people, they'll be tagged with their Slack handle
SLACK_HANDLES=Tristan Kaiser:@tristan, April Smith:@april, George Park:@george

# Or use a JSON file instead (create config/team-handles.json)
SLACK_HANDLES_FILE=

# Default Slack channel for posting (future feature)
SLACK_CHANNEL_DEFAULT=#project-updates

# ============================================================================
# MEETING PROCESSING PREFERENCES
# ============================================================================

# Minimum meeting length in characters to process (skip very short meetings)
MIN_MEETING_LENGTH=100

# Skip meetings with these keywords in the title (comma-separated)
SKIP_RECURRING_KEYWORDS=Daily Standup,1-on-1,Team Sync

# Auto-detect high priority based on keywords (comma-separated)
PRIORITY_KEYWORDS=urgent,blocker,asap,critical

# ============================================================================
# OUTPUT CUSTOMIZATION
# ============================================================================

# Include timestamps in output files
INCLUDE_TIMESTAMPS=true

# Include Granola document IDs in output
INCLUDE_DOCUMENT_ID=true

# Summary output style: slack, markdown, linear, jira
SUMMARY_STYLE=slack

# ============================================================================
# DEVELOPMENT TICKET PREFERENCES
# ============================================================================

# Ticket types to recognize (comma-separated)
DEV_TICKET_TYPES=backend,frontend,design,qa,devops

# Default labels for tickets (comma-separated)
DEFAULT_TICKET_LABELS=pm-agent,auto-generated

# ============================================================================
# GRANOLA CONFIGURATION
# ============================================================================

GRANOLA_CREDENTIALS_PATH=~/Library/Application Support/Granola/supabase.json

# ============================================================================
# OUTPUT CONFIGURATION
# ============================================================================

OUTPUT_DIR=./outputs
